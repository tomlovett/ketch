<div class="layout-column">
  <div class="flex-10 layout-row">
    <span class="flex"></span>
    
    {{#paper-button class="white flex-33"}}
      {{#link-to 'game.recap.points'}}View Points{{/link-to}}
    {{/paper-button}}

    <span class="flex"></span>

    {{#paper-button class="white flex-33"}}
      {{#link-to 'game.recap.players'}}View Players{{/link-to}}
    {{/paper-button}}
    
    <span class="flex"></span>
  </div>

  {{outlet}}
  {{!-- pass in game, team, points, players --}}
  {{!-- components --}}

{{!--   {{#each players as |player|}}
    {{game/recap/player-slice
        player=player
        points=points
    }}
  {{/each}} --}}

  {{!-- else each sortedPoints as point --}}

  <div class="flex"></div>

  <div class="flex-10 layout-row">
    <span class="flex"></span>
    {{#paper-button class="white flex-80"}}
      {{#link-to 'game.play'}}Back to Game{{/link-to}}
    {{/paper-button}}
    <span class="flex"></span>
  </div>
</div>

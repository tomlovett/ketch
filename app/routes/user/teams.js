import Route from '@ember/routing/route';

export default Route.extend({
  // afterModel() {
  //   if (!this.get('model.player.id')) { // sending every time
  //     this.transitionTo('user.player');
  //   }
  // },
});
